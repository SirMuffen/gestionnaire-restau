# gestionnaire-restau
this is basically a language C project where It simulates a system that lets the user add or delete an order and on top of that the "exclusive" mode where it ignores other casual orders and puts the exclusive ones on top of the list 
